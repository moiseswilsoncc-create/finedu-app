### 🧱 Milestone: flujo automático de permisos validado

- Se confirma que el trigger `trigger_insertar_permisos` en la tabla `usuarios` está activo y funcional
- La función `asignar_permisos_usuario()` ejecuta correctamente la inserción de permisos
- Se validan los accesos autorizados en la tabla `permisos_usuario` para el usuario insertado
- Se institucionaliza el flujo de permisos como parte del onboarding técnico
- Se garantiza compatibilidad con el hook `usePermisos` y el render condicional en `App.tsx`

### 🧱 Registro de intentos fallidos y cambios repetidos

- Se introdujo el hook `usePermisos` para modularizar la visibilidad de módulos
- Se detectó que el `usuarioId` no siempre coincide con el `usuario_id` en Supabase
- Se intentó corregir el render condicional en `App.tsx` más de 6 veces sin éxito
- Se verificó el trigger y la función `asignar_permisos_usuario`, que funcionan correctamente
- Se confirmó que los permisos existen en la tabla `permisos_usuario`
- El frontend sigue sin mostrar los módulos, lo que indica un fallo en la lectura o sincronización del `id`
- Se repitieron los pasos de validación sin trazabilidad clara, generando desgaste emocional y técnico

graph TD
A[Usuario se registra en Auth] --> B[Correo de verificación enviado]
B --> C[Usuario confirma correo]
C --> D[Se crea registro en tabla `usuarios`]
D --> E[Trigger asigna permisos automáticamente]
E --> F[Frontend detecta módulos vía `usePermisos`]

### 🧱 Milestone: desacoplamiento del flujo de registro y verificación

- Se detecta que el sistema intenta validar por correo a un usuario que aún no tiene registro en la tabla `usuarios`
- Se confirma que esto rompe la sincronización entre Supabase Auth y la lógica de permisos
- Se institucionaliza el desacoplamiento del flujo:
  - Registro en Auth → verificación por correo
  - Luego inserción en `usuarios` → activación de permisos
- Se documenta que el trigger de permisos depende de la tabla `usuarios`, no de Auth

### 🧱 Milestone: validación del entorno en modo desarrollo

- Se ejecuta `npm run dev` con éxito
- El navegador abre automáticamente en `http://localhost:5173/`
- Se muestra la pantalla de bienvenida sin errores
- Se confirma que el entorno está limpio, funcional y listo para restaurar el usuario

### 🧱 Milestone: validación final de permisos institucionales

- Se confirma que el trigger `trigger_insertar_permisos` ejecutó correctamente la función `asignar_permisos_usuario`
- El usuario `f27be1e5-16e4-441a-ac5b-1d02b3ab5bfd` tiene todos los módulos habilitados con permiso `acceso`
- Se valida que el sistema puede reactivar `usePermisos` en frontend sin bloqueos
- Se institucionaliza este paso como parte del protocolo de restauración y trazabilidad

### 🧱 Milestone: primera señal visual de recuperación en producción

- Se despliega correctamente la aplicación en Vercel con render activo
- Se visualiza el cuadro de bienvenida institucional con botones de ingreso por rol
- Se confirma que desacoplar `usePermisos` permitió restaurar el render
- Se institucionaliza este momento como señal de recuperación y avance emocional

