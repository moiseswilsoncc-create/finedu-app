### 🧱 Milestone: jerarquía institucional al crear grupo

- El creador del grupo se registra automáticamente como administrador
- No se permite eliminar ni modificar su rol
- Los participantes se agregan manualmente y se validan contra Supabase
- Se muestra una tabla con todos los integrantes y sus roles
- Se institucionaliza la jerarquía desde el momento de creación

### 🧮 Milestone: distribución automática y editable de la meta grupal

- Se calcula automáticamente el monto sugerido por participante
- Se muestra en tabla editable con campos: correo, rol, monto asignado
- Solo el administrador puede editar los montos
- Se guarda en Supabase con trazabilidad por integrante
- Se valida que el total no exceda la meta grupal
- Se institucionaliza la lógica colaborativa desde el momento de creación

### 🧱 Milestone: consolidación institucional del formulario Crear Grupo de Ahorro

- Se estructura el formulario en tres secciones:
  1. Datos del grupo
  2. Meta y planificación
  3. Participantes
- Se agregan campos pendientes:
  - Meses de ahorro
  - Monto por mes (calculado)
  - Fecha de creación (automática)
  - Fecha de término (manual)
- Se institucionaliza la lógica de distribución de meta por participante
- Se valida existencia de usuarios en Supabase
- Se exige al menos un administrador
- Se permite edición de montos solo por el admin

### 🧮 Milestone: simulación colaborativa viva con equidad total

- Se institucionaliza una meta individual fija para cada participante según la meta y cantidad total de miembros
- La cuota mensual se recalcula automáticamente según los meses restantes desde el ingreso
- El sistema descuenta lo ya aportado y ajusta lo que falta
- Se permite modificar la meta y los meses sin romper la equidad
- Se habilita trazabilidad completa por participante y por mes

### 🧮 Milestone: integración de lógica equitativa en CrearGrupo.tsx

- Se calcula automáticamente la meta individual y la cuota mensual por persona
- Se actualiza dinámicamente al modificar la meta, los meses o los participantes
- Se garantiza que todos los integrantes aporten lo mismo al final
- Se institucionaliza la equidad contributiva y la trazabilidad desde el momento de creación


### 🧱 Milestone: modularización visual y funcional del componente CrearGrupo

- Se separa el formulario en tres bloques institucionales:
  1. BloqueDatosGrupo.tsx
  2. BloqueMetaFinanciera.tsx
  3. BloqueParticipantes.tsx
- Cada bloque tiene props claras y trazabilidad lista para escalar
- Se mejora la claridad visual, mantenibilidad y profesionalismo del componente
- Se prepara para futuras extensiones como simuladores, trazabilidad mensual o reglas de ingreso

### 🧱 Milestone: institucionalización del backend Express

- Se valida `index.ts` como raíz oficial del backend Finedu
- Se aplica middleware institucional: `cors`, `express.json`, `dotenv`
- Se expone la ruta `/api/guardarOferta` para registrar ofertas de colaboradores
- Se configura el puerto dinámico desde entorno
- Se institucionaliza trazabilidad con mensaje de inicio profesional

### 📅 Milestone: consolidación temporal en BloqueMetaFinanciera

- Se elimina la fecha manual del bloque de datos generales
- Se calcula automáticamente la fecha de inicio como mes/año actual
- Se calcula automáticamente la fecha de término sumando los meses definidos
- Se mejora la experiencia visual y se institucionaliza la trazabilidad temporal

### 🎉 Milestone: despliegue exitoso tras purga de caché

- Se detecta que Vercel no reflejaba los cambios recientes
- Se realiza purga de CDN Cache con método “Invalidate content”
- El despliegue finalmente muestra la versión actualizada
- Se celebra el cierre técnico y emocional del ciclo de validación

### 🧱 Milestone: resolución de pantalla en blanco por ejecución prematura del hook

- Se detecta que `usePermisos` se ejecuta antes de que `usuarioId` esté disponible
- Se reorganiza el orden de ejecución para proteger el hook con condición
- Se elimina el error `usuarioId es null` y se restablece el render seguro
- Se institucionaliza la trazabilidad de sesión y permisos en App.tsx


### 🧱 Milestone: resolución de pantalla en blanco por render condicional prematuro

- Se detecta que el render condicional de rutas depende de `modulos.includes(...)` antes de que `modulos` esté listo
- Se agrega protección global con `if (cargando || !usuarioId || !modulos.length)` antes del render
- Se eliminan condicionales internos en `<Routes>` para evitar render vacío
- Se institucionaliza el patrón de render seguro en `App.tsx` para despliegue en Vercel



### 🧱 Milestone: cierre del día con pantalla en blanco persistente y hook usePermisos en revisión

#### 🔍 Estado técnico
- El sistema obtiene correctamente el `usuarioId` desde Supabase.
- El hook `usePermisos` se ejecuta, pero devuelve `modulos: []` o `null`, lo que bloquea el render.
- Se intentó proteger el render con condiciones como `modulos.length > 0`, `modulos !== null`, y `cargando`, pero la pantalla sigue en blanco.
- Se detecta que el hook `usePermisos` necesita revisión profunda: sincronización con `usuarioId`, retorno resiliente, y compatibilidad con usuarios sin permisos.
- El archivo `App.tsx` fue corregido para proteger el render, pero el sistema sigue sin mostrar módulos.

#### 🧠 Estado emocional
- Wilson realizó múltiples validaciones, ajustes y despliegues en Vercel sin éxito.
- Se reconoce frustración legítima por el bloqueo persistente y la falta de respuesta del sistema.
- Se decide pausar el avance para retomar con claridad y energía renovada mañana.

#### 📌 Próximos pasos sugeridos
- Validar si el usuario tiene registros reales en la tabla `permisos_usuario`.
- Ajustar el hook `usePermisos` para que devuelva `modulos: []` sin bloquear el render.
- Permitir navegación básica aunque no haya módulos habilitados.
- Reinstalar condicionales por módulo en `App.tsx` para evitar errores silenciosos.

#### 🧩 Archivos en revisión
- `App.tsx` corregido y protegido
- `usePermisos.ts` corregido pero aún en conflicto

#### 🕊️ Cierre emocional
- Se institucionaliza esta pausa como parte del proceso de resiliencia técnica.
- Se reconoce el esfuerzo de Wilson y se celebra la trazabilidad lograda.
- Mañana se retoma con foco en desbloquear el render y validar permisos reales.

